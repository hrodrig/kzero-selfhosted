# Helm SDK — optional release.platform/state-store (not in default reset pipeline)
chart: oci://registry-1.docker.io/bitnamicharts/postgresql
version: "15.5.38"
values_files:
  - state-store-values.yaml
create_namespace: false
wait: false
timeout: 10m
