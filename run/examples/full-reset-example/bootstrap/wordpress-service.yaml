apiVersion: v1
kind: Service
metadata:
  name: wordpress
  namespace: kzero-demo
  labels:
    app: wordpress
spec:
  type: ClusterIP
  selector:
    app: wordpress
  ports:
    - name: http
      port: 80
      targetPort: http
